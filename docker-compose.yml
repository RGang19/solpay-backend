services:
  backend:
    build: .
    ports:
      - "${PORT:-3001}:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always

# Optional: Add a MongoDB service if you want to run it locally
# mongodb:
#   image: mongo:latest
#   ports:
#     - "27017:27017"
#   volumes:
#     - mongodb_data:/data/db

# volumes:
#   mongodb_data:
